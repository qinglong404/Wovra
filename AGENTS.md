# AGENTS.md — 工作区指令（Wovra 仓库）

## 0. 省步数（最高优先级，用户体感第一）

用户看得见每一步的往返延迟。同一件事拆成八步问完，就是"每次下命令都要搞好久"。
以下三条是硬纪律，违反按交付缺陷处理：

1. **先跑仪器，再考虑写脚本。** 想知道"为什么又触发了 / 地板多大 / 缓存命中多少 /
   账本多厚"，直接跑 `uv run --no-sync python scripts/maint_health.py [task_id]`。
   动手写新脚本前先 `search_files` 查 `docs/worklog-20260911.md` 与 `docs/*-intent.md`
   里的既有结论。只有"确实没有记录的新事实"才允许写新脚本。
2. **一次取证、多处引用。** 一次取证的结论落进 worklog 某一节（或固化成仪器），
   后续全部指向它，不重跑。同一事实测第二遍必须说明上一次读数为何作废。
3. **脚本不删。** 取证脚本一律留着（用户口径，2026-09-11："测试脚本，不要删"）——
   别让同一个测量下次再写一遍。反复用得到的固化成 `scripts/` 常驻仪器
   （如 `scripts/maint_health.py`）；一次性的也保留，不放手里删；凡是有长期诊断
   价值的都进 `scripts/`（纳版本库，换机器还在）。
   脚本输出只打结论（≤20 行），禁止 dump 原始记录。

## 1. 动手纪律

- 改功能用 `edit_file` 增量改对应模块；`write_file` 全量覆盖只允许用于首次创建文件。
- 每批改动收尾：追加 `docs/worklog-20260911.md` 一节（动机 / 涉及文件 / 实测证据 / 回滚方式），
  跑全套测试全绿后再提交推送。
- 跑测试必须带 `--no-sync`：`uv run --no-sync python -m pytest -q`。
  不带时 uv 会重建包，撞上正在运行的 `wovra.exe`（WinError 32）。
- 工具输出默认**全量返回**（上限 200,000 字符，`WOVRA_OUTPUT_LIMIT` 可调）；
  真超限时**不丢文本也不硬塞**：返回开头预览（`WOVRA_PREVIEW_CHARS`，默认 2,000）
  + 原文体量（字符/行数）+ 落盘路径 `output/spill/`，完整内容随时 `read_file` 取回
  （read_file 的原文就在盘上，直接给继续读取位置，不落副本）。
  口径（用户 2026-09-11）：「不要截断丢失文本，量大可以不进上下文，
  返回截断内容但可以让你取回，告诉它有多大就可以」。
- 读文件用大 `num_lines` 一次读完（单次上限 20,000 行），不要切很多小段反复读。

## 2. 两条不准触碰的硬线

- **缓存前缀纪律**：只有整理生效（promotion）才允许破坏前缀。
  装配不得在轮次中部改写历史字节——那是被实测判死的 V2 三档滑窗（命中率 83.7% → 49.9%）。
- **tools 数组恒定**：提交工具常驻，所有请求的 tools 序列化必须一致，
  否则整段前缀缓存作废（与具体工具无关，一差分叉即全丢）。

## 3. 别碰

- `.wovra/`（本地越界授权清单）、`.env`（密钥）、`tasks/*`（会话数据，已入库的数据资产除外）。
- 活跃会话的 `task.json` 不手工改：运行中进程内存握着旧数据，会被下次落盘覆盖回去。
  能靠加载期自愈的，就写进 `Task.load`。
- 破坏性 git 操作（`git reset --hard`、`git clean -fd`、强制推送）必须先问用户。
